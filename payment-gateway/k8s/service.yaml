apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-svc
  namespace: payment-gateway-prod
  labels:
    app: payment-gateway
spec:
  type: ClusterIP
  selector:
    app: payment-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
